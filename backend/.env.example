# KeLiva Backend Environment Variables

# Groq API Configuration (FREE - 14,000 requests/day for 8B model)
GROQ_API_KEY=your_groq_api_key_here

# Telegram Bot API Configuration (100% FREE - RECOMMENDED)
# Get your bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_WEBHOOK_URL=https://your-app.onrender.com/api/telegram/webhook
# Optional: Secret token for webhook validation (recommended for security)
TELEGRAM_SECRET_TOKEN=your_random_secret_token_here

# Twilio Configuration (OPTIONAL - for WhatsApp integration)
# Note: Twilio has a free trial with $15 credit
# Only configure if you need WhatsApp in addition to Telegram
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_PHONE_NUMBER=your_twilio_phone_number_here

# Speech Services Configuration
# Using FREE alternatives:
# - STT: Web Speech API (browser-based, runs client-side)
# - TTS: Edge TTS (completely free, runs server-side)
# Note: Vosk NOT used (requires local deployment, incompatible with Render)
USE_WEB_SPEECH_API=true
USE_EDGE_TTS=true

# Database Configuration
# For LOCAL development (SQLite):
# DATABASE_URL=sqlite:///./keliva.db

# For PRODUCTION on Render (PostgreSQL - REQUIRED):
# Option 1: Supabase (FREE forever, 500MB)
# DATABASE_URL=postgresql://user:password@db.supabase.co:5432/postgres
# Get from: https://supabase.com/ (create project, copy connection string)

# Option 2: Render PostgreSQL (FREE for 90 days)
# DATABASE_URL=postgresql://user:password@dpg-xxxxx.render.com/dbname
# Get from: Render Dashboard > New > PostgreSQL

DATABASE_URL=postgresql://user:password@host:5432/dbname

# Server Configuration
PORT=8000
HOST=0.0.0.0
